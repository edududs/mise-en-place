"""AULA 4 — o salão vivo: a clientela chega, decide e vai embora.

Snapshot fechado da aula: rode com `python3 -m ex4`.

É a aula 3 inteira (o package `restaurante/`, sem alteração) mais o package
`clientela/`. O diff entre `ex3/` e `ex4/` É a aula.
"""

from __future__ import annotations
